import { createStore } from 'vuex'

export default createStore({
  state: {
    baseUrl: process.env.VUE_APP_BASE_URL,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
